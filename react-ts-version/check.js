const { initializeApp } = require('firebase/app'); const { getDatabase, ref, get } = require('firebase/database'); const firebaseConfig = { databaseURL: 'https://mathmaticore-default-rtdb.europe-west1.firebasedatabase.app/' }; const app = initializeApp(firebaseConfig); const db = getDatabase(app); get(ref(db, 'radar_alerts')).then(s = console.log('Radar alerts count:', s.val() ? Object.keys(s.val()).length : 0); process.exit(0); }).catch(e = console.error(e); process.exit(1); });  
