{
  "rules": {
    ".read": false,
    ".write": false,

    "schools": {
      ".read": "auth != null",
      ".write": "auth != null && (auth.token.role === 'admin' || auth.token.admin === true)"
    },

    "public_classes": {
      ".read": true,
      ".write": "auth != null && (auth.token.role === 'admin' || auth.token.admin === true)"
    },

    "classes": {
      ".read": "auth != null",
      ".write": "auth != null && (auth.token.role === 'admin' || auth.token.admin === true)"
    },

    "users": {
      "students": {
        ".read": "auth != null && (auth.token.role === 'teacher' || auth.token.teacher === true || auth.token.role === 'admin' || auth.token.admin === true || root.child('users/teachers/' + auth.uid).exists())",
        "$studentId": {
          ".read": "auth != null && (auth.uid === $studentId || auth.token.role === 'teacher' || auth.token.teacher === true || auth.token.role === 'admin' || auth.token.admin === true || root.child('users/teachers/' + auth.uid).exists())",
          ".write": "auth != null && (auth.uid === $studentId || auth.token.role === 'teacher' || auth.token.teacher === true || auth.token.role === 'admin' || auth.token.admin === true || root.child('users/teachers/' + auth.uid).exists())"
        }
      },
      "teachers": {
        ".read": "auth != null",
        "$teacherId": {
          ".read": "auth != null",
          ".write": "auth != null && (auth.uid === $teacherId || auth.token.role === 'admin' || auth.token.admin === true || !data.exists())"
        }
      }
    },

    "students": {
      ".read": "auth != null",
      "$studentId": {
        ".read": "auth != null",
        ".write": "auth != null && (auth.uid === $studentId || auth.token.role === 'teacher' || auth.token.teacher === true || auth.token.role === 'admin' || auth.token.admin === true || root.child('users/teachers/' + auth.uid).exists())"
      }
    },

    "radar_alerts": {
      ".read": "auth != null",
      "$alertId": {
        ".write": "auth != null"
      }
    },

    "reflections": {
      ".read": "auth != null",
      "$reflectionId": {
        ".write": "auth != null"
      }
    },

    "chat_messages": {
      ".read": "auth != null",
      "$roomId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },

    "sessions": {
      ".read": "auth != null",
      ".write": "auth != null && (auth.token.role === 'teacher' || auth.token.teacher === true || auth.token.role === 'admin' || auth.token.admin === true || root.child('users/teachers/' + auth.uid).exists())"
    },

    "replays": {
      ".read": "auth != null",
      "$sessionKey": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },

    "chat": {
      "$roomId": {
        ".read": "auth != null",
        "$messageId": {
          ".write": "auth != null"
        }
      }
    },

    "approved_tasks": {
      "$studentId": {
        ".read": "auth != null",
        ".write": "auth != null && (auth.token.role === 'teacher' || auth.token.teacher === true || auth.token.role === 'admin' || auth.token.admin === true)"
      }
    },

    "ai_pending_approvals": {
      "$teacherId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },

    "system_control": {
      ".read": true,
      ".write": "auth != null && (auth.token.role === 'admin' || auth.token.admin === true)"
    },

    "active_class_session": {
      ".read": true,
      ".write": "auth != null && (auth.token.role === 'teacher' || auth.token.teacher === true || auth.token.role === 'admin' || auth.token.admin === true || root.child('users/teachers/' + auth.uid).exists())"
    },

    "audit_logs": {
      ".read": "auth != null && (auth.token.role === 'admin' || auth.token.admin === true)",
      ".write": "auth != null",
      ".indexOn": ["timestamp"]
    }
  }
}
