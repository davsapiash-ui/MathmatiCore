{
  "rules": {
    ".read": false,
    ".write": false,

    "schools": {
      ".read": "auth != null",
      ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.email === 'admin@mathmaticore.local')"
    },

    "public_classes": {
      ".read": true,
      ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.email === 'admin@mathmaticore.local')"
    },

    "classes": {
      ".read": "auth != null",
      ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.email === 'admin@mathmaticore.local')"
    },

    "users": {
      "students": {
        ".read": "auth != null && (auth.token.teacher === true || auth.token.admin === true || auth.token.role === 'teacher' || auth.token.role === 'admin' || auth.token.email === 'admin@mathmaticore.local' || root.child('users/teachers/' + auth.uid).exists())",
        "$studentId": {
          ".read": "auth != null && (auth.uid === $studentId || auth.token.email === $studentId + '@mathmaticore.local' || auth.token.teacher === true || auth.token.admin === true || auth.token.role === 'teacher' || auth.token.role === 'admin' || auth.token.email === 'admin@mathmaticore.local' || root.child('users/teachers/' + auth.uid).exists())",
          ".write": "auth != null && (auth.uid === $studentId || auth.token.email === $studentId + '@mathmaticore.local' || auth.token.teacher === true || auth.token.admin === true || auth.token.role === 'teacher' || auth.token.role === 'admin' || auth.token.email === 'admin@mathmaticore.local' || root.child('users/teachers/' + auth.uid).exists())"
        }
      },
      "teachers": {
        ".read": "auth != null",
        "$teacherId": {
          ".read": "auth != null",
          ".write": "auth != null && (auth.uid === $teacherId || auth.token.admin === true || auth.token.role === 'admin' || auth.token.email === 'admin@mathmaticore.local' || !data.exists())"
        }
      }
    },

    "students": {
      ".read": "auth != null",
      "$studentId": {
        ".read": "auth != null",
        ".write": "auth != null && (auth.uid === $studentId || auth.token.teacher === true || auth.token.admin === true || auth.token.role === 'teacher' || auth.token.role === 'admin' || auth.token.email === 'admin@mathmaticore.local' || root.child('users/teachers/' + auth.uid).exists())"
      }
    },

    "radar_alerts": {
      ".read": "auth != null",
      "$alertId": {
        ".write": "auth != null"
      }
    },

    "reflections": {
      ".read": "auth != null",
      "$reflectionId": {
        ".write": "auth != null"
      }
    },

    "chat_messages": {
      ".read": "auth != null",
      "$roomId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },

    "sessions": {
      ".read": "auth != null",
      ".write": "auth != null && (auth.token.teacher === true || auth.token.admin === true || auth.token.role === 'teacher' || auth.token.role === 'admin' || auth.token.email === 'admin@mathmaticore.local' || root.child('users/teachers/' + auth.uid).exists())"
    },

    "replays": {
      ".read": "auth != null",
      "$sessionKey": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },

    "chat": {
      "$roomId": {
        ".read": "auth != null",
        "$messageId": {
          ".write": "auth != null"
        }
      }
    },

    "approved_tasks": {
      "$studentId": {
        ".read": "auth != null",
        ".write": "auth != null && (auth.token.teacher === true || auth.token.admin === true || auth.token.role === 'teacher' || auth.token.role === 'admin' || auth.token.email === 'admin@mathmaticore.local')"
      }
    },

    "ai_pending_approvals": {
      "$teacherId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },

    "system_control": {
      ".read": true,
      ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.email === 'admin@mathmaticore.local')"
    },

    "active_class_session": {
      ".read": true,
      ".write": "auth != null && (auth.token.teacher === true || auth.token.admin === true || auth.token.role === 'teacher' || auth.token.role === 'admin' || auth.token.email === 'admin@mathmaticore.local' || root.child('users/teachers/' + auth.uid).exists())"
    },

    "audit_logs": {
      ".read": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.email === 'admin@mathmaticore.local')",
      ".write": "auth != null",
      ".indexOn": ["timestamp"]
    }
  }
}
