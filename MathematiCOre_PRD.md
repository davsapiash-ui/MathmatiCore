# מסמך דרישות פיתוח ומפרט מערכת (PRD) – פלטפורמת "מתמטיקאור" (MathematiCOre)
## מפרט מוצר טכנו-פדגוגי מאוחד וסופי – גרסה v7 (גרסה מטוהרת ומעודכנת) (World-Class Executable Specification)
### מיועד להזנה ישירה כסוכן בינה מלאכותית / מערכת הנחיות עבור Google Antigravity 2.0

מסמך זה מהווה מפרט דרישות מוצר טכנו-פדגוגי רשמי, קשיח ומאוחד (PRD / Executable Specification) עבור פלטפורמת **"מתמטיקאור" (MathematiCOre)"**. המפרט מתוכנן במיוחד כדי לשמש כהוראות מערכת קבועות (System Instructions / Prompt Engineering Specs) עבור סוכן פיתוח אוטונומי, ומגדיר את חוקי הברזל הפדגוגיים, הממשקיים, והבטיחותיים של המערכת "עד רמת הפסיק" במטרה להוביל את תחום הטכנולוגיה החינוכית לרמה הבינלאומית הגבוהה ביותר.

---

## 1. הגדרת זהות המערכת, קהל היעד והרציונל הפדגוגי

### 1.1. זהות המוצר והשם (Product Identity)
*   **שם הפרויקט:** "מתמטיקאור" (MathematiCOre).
*   **משמעות השם:** משלב את המילה "אור" (הארת תהליכי החשיבה והוויסות הסמויים של הלומד) עם המילה האנגלית "COre" (הליבה של מודל ה-TPACK - שילוב מדויק בין טכנולוגיה, פדגוגיה ותוכן). השם נכתב באותיות גדולות (COre) כדי להדגיש את החיבור הליבתי האקדמי הזה [1].

### 1.2. קהל היעד והקשר הלמידה (Target Audience & Context)
*   **אוכלוסיית היעד:** תלמידי כיתה ג' בחינוך הרגיל ובחינוך המיוחד, עם התאמה מובנית וקשיחה לתלמידים על רצף האוטיזם (ASD) בתפקוד גבוה [1].
*   **מודל הפעלה היברידי (Hybrid Classroom Model):** מודל מוגדר וקשיח השומר על איזון בין העולם הדיגיטלי לפיזי. מתוך שיעור של 45 דקות ברוטו, רק **20 עד 30 דקות** (ממוצע של 25 דקות) מוקדשות לעבודה עצמאית נטו של התלמיד מול התוכנה הדיגיטלית, ויתרת השיעור מנוצלת להקניה, שיח כיתתי פיזי ותיווך אנושי בהובלת המורה [1, 21, 31, 32]. החלטה זו מונעת עומס קוגניטיבי ושפתי על לומדי ASD ומבטיחה שהטכנולוגיה תשמש ככלי מעצים ומקדם קשר אנושי ולא כמסך מבודד [32].

### 1.3. אתגר הליבה (The Core Challenge)
*   **מצד התלמיד:** מעבר מלמידה מכנית ופסיבית (Rote Learning and Passive Compliance), שבה תלמידים מתמקדים בהשלמה פרוצדורלית של מטלות ללא הבנה מושגית עמוקה, אל עבר תובנה תהליכית עמוקה ופיתוח תובנה מספרית (Number Sense) [2, 89, 566].
*   **מצד המורה:** יצירת תשתית ניהול ולוח בקרה (דשבורד) המאפשרים להפוך את תהליך הלמידה וחשיפת השגיאות הסמויות של התלמיד לשקופים לחלוטין בזמן אמת, תוך ניהול קשב מונחה נתונים בכיתה ההיברידית [2, 89].

---

## 2. חמשת עקרונות העיצוב הפדגוגיים של המערכת (Pedagogical Pillars)

1.  **מעבר CRA דיגיטלי (Concrete-Representational-Abstract):** גישור קוגניטיבי דו-כיווני בין ייצוג קונקרטי (גרירה ופירוק של לבני דינס דינמיות על גבי המסך) לייצוג חזותי ייצוגי (אנימציית המרה ופריטה חלקה) ולייצוג סמלי מופשט (הקלדת הספרות בבית המספרים הטורי). המעבר אינו חד-כיווני, אלא אדפטיבי ודו-כיווני: המערכת מונעת כניסת תלמידים לפרוצדורות מכניות בבית המספרים ללא הבנה מושגית קונקרטית מוקדמת [9, 33, 90, 566].
2.  **פיתוח הכוונה עצמית וויסות למידה (Self-Regulated Learning - SRL):** עידוד סוכנות לומד אקטיבית דרך מנגנון 'בחירה מכוילת' (Calibrated Choice) המאפשר לתלמיד להחזיר פיגומים ועזרי עוגן חזותיים שדעכו באופן יזום וזמני בלחיצת כפתור אקטיבית [5, 9, 35, 90]. שילוב כפתור ה-Undo הפדגוגי כרשת ביטחון רגשית המעודדת ניסוי וטעייה עצמאיים ללא חרדה, ואינו נספר כשגיאה מתמטית אלא כאינדיקציה חיובית לוויסות עצמי אקטיבי [9, 35, 90].
3.  **עיצוב אוניברסלי ללמידה (Universal Design for Learning - UDL):** ממשק מינימליסטי ונקי לחלוטין ממסיחים קשביים חזותיים או מוטוריים [9, 36, 90]. כלי עזר דיגיטליים נחשפים בהדרגה וננעלים במנעול חזותי עדין כאשר אינם רלוונטיים, למניעת הצפה חזותית וקשבית אצל לומדי ASD ושילוב [9, 36, 90].
4.  **חונכות סוקרטית בממשק סגור (Zero-Chatbot Policy):** הימנעות מוחלטת מצ'אטבוטים מילוליים פתוחים הפוגעים ברגולציית הפרטיות של ילדים מתחת לגיל 13 ומייצרים עומס שפתי קיצוני אצל ASD [4, 9, 37, 90]. בעת שגיאה, המערכת משהה תגובה ומציגה שאלת הכוונה סוקרטית קצרה בממשק סגור (שאלת רב-ברירה סגורה), המזמינה את התלמיד לבצע בקרה עצמית ללא הצפה [9, 37, 90].
5.  **רדאר פדגוגי שקט ודשבורד מורה:** ניטור סמוי וחרישי של מיקרו-פעולות הלומד ללא שעוני עצר גלויים המייצרים חרדה, ותרגומם לתובנות פדגוגיות קצרות בשפה טבעית עבור המורה לצורך התערבות אנושית ממוקדת [9, 38, 90].

---

## 3. רצף 8 המפגשים: מבנה, אדפטיביות וחסימות (The 8-Session Sequence)

המערכת מיישמת מסלול מדורג בן 8 מפגשים (שעות אקדמיות) המאזן באופן קשיח בין חקר, אבחון ותיווך [9, 22]:

```
[מפגש 1: ארגז החול] ➔ [מפגש 2: אבחון מעוף הדבורה] ➔ 🛑 [שער אישור מורה - Approval Gate]
                                                             │
                                         ┌───────────────────┴───────────────────┐
                                         ▼                                       ▼
                       [מסלול ירוק - רמת כיתה ג']            [מסלול צמצום פערי קדם - א'-ב']
                                         │                                       │
                                         └───────────────────┬───────────────────┘
                                                             ▼
                                                [מפגשים 3-7: למידה אדפטיבית]
                                                             │
                                                             ▼
                                                [מפגש 8: הערכה מופשטת ו-SRL]
```

### 3.1. שלב א': חקר ואבחון שקט (מפגשים 1–2)
*   **מפגש 1 – ארגז החול הדיגיטלי המונחה (Sandbox & Priming):**
    *   **מטרה:** היכרות חופשית ללא לחץ וציונים עם כלי הממשק (לבני דינס, בית מספרים, כפתור Undo), הפחתת חרדת ממשק וריענון קוגניטיבי שקוע של המבנה העשרוני בתחום ה-1,000 (רמת סוף כיתה ב') [9, 25, 41].
    *   **אבטחה ו-UDL:** הממשק מותאם למניעת עומס קשבי התחלתי [9, 41]. אין מטרות חישוביות קשיחות או התערבות מצד מנוע ה-AI [9, 41].
*   **מפגש 2 – אבחון "מעוף הדבורה" (אבחון שקט רב-ממדי בתחום ה-1,000):**
    *   **מטרה:** מיפוי חרישי וסמוי של הבנת התלמיד בכל עמודי התווך של המטריקס בתחום ה-1,000, כדי לאתר במדויק את שורש הטעות ההיסטורי (פערי קדם מכיתות א' ו-ב') [9, 26, 42].
    *   **חוקי ברזל לאבחון נקי:** פתרון סדרת משימות ללא שום עזרה אקטיבית, רמזים, או פיגומים [9, 43]. **מנגנון ה-Auto-Unlock וכל מנגנוני הפיגומים והמעקפים (CRA Bypass) מנוטרלים לחלוטין ובאופן קשיח במהלך מפגש זה [9, 43].** אין שעוני עצר גלויים או ציונים חיצוניים המייצרים חרדה [9, 59].

### 3.2. שלב ב': שער אישור המורה (Approval Gate)
*   **חסימה מערכתית מוחלטת:** נעילה מערכתית קשיחה של תוכנת התלמיד מיד בתום מפגש 2 [9, 45].
*   **פעולת המורה:** המורה שרה (שם בדוי) מזדהה ב-SSO מחוזי מאובטח (בדיקת דומיין מחוזי @edu-haifa.org.il + אימות מול רשימת המורשים הדינמית Whitelist ב-Firestore שהוזנה ידנית על ידי מנהל המערכת גלעד), בוחנת את דוחות האבחון של "מעוף הדבורה" ומאשרת ידנית את מסלול הלמידה המותאם לכל תלמיד להמשך הרצף [9, 23, 45]:
    1.  **המסלול הירוק:** רמת כיתה ג' (תחום הרבבה עד 10,000) [9, 23].
    2.  **מסלול צמצום פערי קדם:** חזרה אדפטיבית לרמות כיתות א' ו-ב' (תחומי ה-100 וה-1,000) [9, 23].
*   **מסך נעילה ידידותי במפגש 2 (Friendly Lock Gate):** כדי למנוע חרדה ודפוסי חוסר אונים נרכש אצל לומדים חרדתיים, חל איסור להציג שלט חסימה מאיים בסיום מפגש 2 [107]. במקום זאת, המערכת תציג מסך סיום חיובי ומעודד בלשון הבאה: "המורה בודקת את מעוף הדבורה שלך. נתחיל בקרוב במפגש 3!" [107].

### 3.3. שלב ג': למידה אדפטיבית ותיווך (מפגשים 3–7)
*   **מפגש 3 – ערך המקום וגמישות ייצוגית (פירוק והרכבה):** פיתוח גמישות ייצוגית בייצוג מספרים (פירוק והרכבה סטנדרטיים ולא-סטנדרטיים). דוגמה: במסלול פערים ייצוג 230 באמצעות 23 עשרות [9, 27, 46]. במסלול הירוק ייצוג 2,300 כ-23 מאות [9, 27, 46].
*   **מפגש 4 – אלגוריתם החיבור במאונך והמרה פשוטה (הקבצה):** ביסוס האלגוריתם והבנת מושג ההמרה דרך מניפולציה חזותית בעזרים, תוך הפעלת ה-CRA Bridge למניעת קריסה של זיכרון העבודה וקשיי שליפה [9, 28, 47].
*   **מפגש 5 – אלגוריתם החיסור במאונך והמרה פשוטה (פריטה):** פתרון תרגילי חיסור עם פריטה בטור היחידות תוך שימוש חופשי ב-Undo כרשת ביטחון רגשית [9, 29, 48].
*   **מפגש 6 – אתגר האפס כשומר מקום ומעבר מעל אפסים (המרה כפולה):** פתרון תרגילי חיסור מורכבים הדורשים פריטה כפולה דרך ספרת האפס (כמו 400 פחות 156) [9, 29, 49]. המערכת מפעילה עמעום טורים דינמי (Visual Focus Masking) למניעת עומס קשבי [9, 29, 49].
*   **מפגש 7 – חיבור וחיסור אתגר בתחום ה-10,000:** חיזוק מיומנויות אלגוריתמיות וגמישות חישובית מורכבת בתחום הרבבה [9, 50].

### 3.4. שלב ד': הערכה ורפלקציה (מפגש 8)
*   **מפגש 8 – הערכה ורפלקציה מסכמת (SRL Reflection):**
    *   **הסרה מלאה של הפיגומים:** מחיקה חזותית מוחלטת של לבני הדינס מהמסך, לבחינת שליפה עצמאית ושליטה מושגית מופשטת [9, 30, 51, 52].
    *   **רשת ביטחון וצמצום פער הדעיכה (Fading Gap):** כפתור ה-Undo נותר פעיל ופתוח לשימוש חופשי ואינו פוגע בציון [34]. בעת זיהוי לולאת ניחושים (מעל 3 לחיצות Undo רצופות בתרגיל בודד), מופעל מנגנון חניכה סוקרטית רך (Soft Socratic - Faded Abstract Prompting) המציג שאלת הכוונה סגורה המדגישה קשרים מבניים (כגון צביעה דיפרנציאלית של ערך הספרה בבית המספרים), כדי לגשר על פער ההפשטה ללא חזרה לרמה הקונקרטית [19, 30, 52, 76].
    *   **מסך רפלקציה SRL תלת-שלבי מובנה [19, 30, 52, 77, 78]:**
        1.  *מדד מאמץ חזותי קוגניטיבי:* בחירה מתוך 3 דמויות דבורה ידידותיות (דבורה נחה - קל מדי, דבורה עובדת בחיוך - מאמץ נכון, דבורה עמוסה - קשה מאוד) [19, 30, 52, 77].
        2.  *חלונית בחירת אסטרטגיה מוצלחת:* שאלה סגורה מרובת אפשרויות ("מה עזר לכם הכי הרבה להצליח היום?") [19, 30, 52, 78].
        3.  *משוב חיובי מעודד התמדה (Process Feedback):* הודעה מילולית המעודדת מאמץ וחקירה עצמאית על בסיס מדד ההתמדה (Persistence Index) שנמדד סמוית ברקע [19, 30, 52, 78].

---

## 4. מפרט טכנולוגי ומכונות מצבים (State Machines & Specs)

### 4.1. מכונת המצבים של בית המספרים (Strict CRA Bridge)
מנגנון ה-Strict CRA Bridge מופעל תחת פרופיל תמיכה מוגבר (ON) המוגדר בדשבורד המורה [62, 119]. המנגנון שולט בהתנהגות המקלדת הדיגיטלית בבית המספרים ומאלץ פעולה קונקרטית בלבני הדינס לפני הקלדה [62, 119]:

*   **מצבי המקלדת (Keyboard States):**
    1.  `LOCKED` (חסומה לקלט, מחזירה רטט/סירוב עדין בלחיצה) [119].
    2.  `UNLOCKED` (פתוחה לקלט חופשי) [119].
    3.  `Socratic Only` (פתוחה אך ורק לצורך מענה על שאלת תיווך סגורה) [119].

*   **חוקי מעבר ותנאים (Transition Rules) [119]:**
    *   **מעבר מ-`LOCKED` ל-`UNLOCKED`:** מתבצע ישירות אך ורק בעקבות אירוע `block_group_success = TRUE` (ביצוע המרה מוצלחת של פריטה/הקבצה בלוח לבני הדינס על ידי התלמיד).
    *   **מעבר מ-`LOCKED` ל-`Socratic Only`:** מופעל אוטומטית בעקבות אירוע טיימר תקיעות `hesitation_timer_expire = TRUE` (חוסר פעילות של 30 שניות או 3 ניסיונות קלט חסומים בטור נעול).
    *   **מעבר מ-`Socratic Only` ל-`UNLOCKED`:** מתבצע אך ורק בעקבות אירוע `Socratic_Success = TRUE` (מענה תקין של התלמיד על שאלת התיווך הרב-ברירה סגורה). **האימות לעולם אינו מאפשר הזנת פתרון ישיר, אלא משחרר את המקלדת לצורך מענה על שאלת תיווך המלווה ברמז חזותי מהבהב, למניעת Gaming the System [21, 82].**
    *   **אתחול נעילה (Undo Reset):** במידה והמקלדת במצב `UNLOCKED` והמשתמש מבצע `undo_click` המבטל את פעולת ההמרה ומחזיר את לוח הדינס למצב הדורש פריטה/הקבצה, המערכת תבצע מעבר מצב מיידי חזרה ל-`LOCKED` למניעת עקיפת הפיגום הדו-כיווני.


### 4.3. יתירות כלי ניווט במערכת (Navigation Redundancy Routing Spec)
נתב האפליקציה (Frontend Router) יתמוך בשני נתיבים לוגיים נפרדים המובילים לאותו רכיב תצוגה (Component) של דוח אבחון 'מעוף הדבורה' של תלמיד אנונימי ספציפי [103, 122]:
1.  **נתיב מבני (Structural Route):** `/reports/student/:id` (גישה מסרגל הניווט הצידי הסטטי בלוח הבקרה) [103, 122].
2.  **נתיב הקשרי (Contextual Route):** `/dashboard/student/:id/view` (גישה בלחיצה ישירה על כרטיס התלמיד הייעודי בגריד הטופוגרפי של הדשבורד הראשי) [103, 122].
*   שני הנתיבים יעברו דרך אותו ה-Route Guard בצד השרת, ויחייבו אימות SSO מלא ובדיקת קיום הרשאת `TEACHER` ב-Token הפעיל, לפני חשיפת הנתונים [122].

### 4.4. בקרת שימוש ב-Undo וספי רגישות (Soft Socratic Loops)
כפתור ה-Undo נותר פעיל תמיד כרשת ביטחון רגשית ללא השהיות או נעילות מסך [83, 88]. בעת זיהוי לולאת ניחושים, מופעל מנגנון "Soft Socratic" המציג שאלת רב-ברירה לרפלקציה עצמית [83, 88]. ספי הרגישות להפעלת מנגנון החניכה הסוקרטית השקטה בעקבות שימוש ב-Undo נחלקים לשניים [83]:
*   **במפגש 6 (סף מקל – 4 לחיצות ב-15 שניות):** שלב הבניית הידע הפעיל בתלת-ספרתי, שבו הבלוקים מופיעים אקטיבית. ה-Undo מייצג ניסוי וטעייה יצרניים, ולכן נדרש חופש פעולה רחב יותר לטעות ללא התערבות חוסמת [83].
*   **במפגש 8 (סף מחמיר – 3 לחיצות בתרגיל בודד):** שלב הערכה מופשטת ללא לבנים. לחיצות Undo חוזרות ונשנות באותו תרגיל מעידות על תסכול עמוק וניסיונות ניחוש אקראיים לעקיפת המטלה (Gaming the System). על כן, נקבע סף מחמיר יותר המפעיל מיידית את "פרוטוקול התיווך הממוקד הדועך" [83].

### 4.5. כפתור עקיפה פיזית למורה (Physical Scaffolding Override)
*   **מנגנון הפעלה:** בדשבורד המורה יתווסף מתג Physical Override ON עבור תלמיד ספציפי [123]. הפעלתו תשדר אירוע `physical_override_active = TRUE` [123].
*   **השפעה על לקוח התלמיד:** עם קבלת האירוע, המערכת תקפיא את טיימר התקיעות (Hesitation Timer), תבטל זמנית את חסימת המקלדת ב-Strict CRA Bridge, ותאפשר קלט חופשי, במקביל לעדכון הדשבורד לסימון עבודה פיזית של התלמיד עם קוביות דינס פיזיות על שולחנו [123].
*   **פרוטוקול סיום העקיפה (Override Teardown State):** כאשר המורה מכבה את מתג העקיפה הפיזית בדשבורד, ישודר אירוע `physical_override_active = FALSE` [123]. עם קבלת אירוע זה, לקוח התלמיד יפעיל מיידית פונקציית אתחול וניקוי (Cleanup Pipeline) שתבצע את הפעולות הבאות [123]:
    1. תפעיל מחדש את טיימר התקיעות [123].
    2. תבצע דגימה מהירה של מצב הבלוקים בדינס [123].
    3. תחזיר את מכונת המצבים של ה-CRA Bridge למצב `LOCKED` במידה והתרגיל הנוכחי דורש המרה שטרם בוצעה דיגיטלית [123].

### 4.6. מעברים אדפטיביים דו-כיווניים (Dual-Gate Adaptive System) וחלון הפיגומים הזמני (Kaleidoscope Scaffolding)
*   **עליית מסלול מבוקרת (Upward Transition):** כדי למנוע ערעור של תחושת המוגנות והעקביות אצל תלמידי ASD, המערכת לעולם לא תבצע מעבר אוטומטי למסלול גבוה יותר באמצע שיעור [87]. במידה והלומד מציג שליטה ושטף פדגוגי לאורך 2 מפגשים רצופים במסלול פערים, המערכת תשלח המלצה מנומקת למורה בדשבורד, והמעבר למסלול הירוק יוחל רק במפגש הבא לאחר אישורו הידני [87].
*   **נסיגת תמיכה מקומית (Downward Scaffolding Window):** במידה ותלמיד במסלול הירוק נתקל בקושי עמוק ביותר (כגון 3 שגיאות רצופות בטור המאות), המערכת לא תשנמך את מסלולו הרשמי באופן מבהיל [87]. במקום זאת, מנוע החוקים יפתח זמנית "חלון תמיכה ממוקד" (Kaleidoscope Scaffolding) המציע פיגום קונקרטי חזותי מתחומי המטריקס של כיתה ב' (תחום ה-1,000) בתוך החניכה הסוקרטית המקומית [87]. עם פתרון הקושי והחזרת השטף, החלון ייסגר והתלמיד ימשיך במסלולו הרשמי ללא תיוג או פגיעה בדימוי העצמי [87].

### 4.7. לוח חיבור אדפטיבי (Symmetrical Addition Grid)
*   **תפקיד פדגוגי:** משמש כ"פיגום עוקף שליפה" (Extraction-Bypass Scaffolding) לביסוס עובדות ולבקרת פתרון בשיעורי חיבור וחיסור [81].
*   **זמינות ומנגנון הופעה:** הכלי זמין במפגש 4 (חיבור במאונך) ובמפגשים 5 ו-6 (חיסור במאונך) [81]. הוא ממוזער ומסתר כברירת מחדל, ויופיע כלוח עזר דינמי באנימציית עמעום רכה (Fade-In של 2.5 שניות) רק בעת זיהוי היסוס (מעל 30 שניות) או טעויות קלט בבית המספרים [81, 107].
*   **לוגיקת אינטראקציה:** הכלי מבוסס על זרימת לחיצות בלבד ללא מצב Hover למניעת קשיים מוטוריים [81, 107]. התלמיד מבצע בחירה דו-שלבית של המחוברים, ולאחר הצגת התוצאה והבהובה, החלונית מתעמעמת ונסגרת אוטומטית כעבור 3 שניות [81, 107].
*   **מודוס חקירה:** בבחירת סכום, הלוח מבליט את אלכסון הצירופים האפשריים ומחייב לחיצה אקטיבית על המשבצות לגילוי המחוברים, לשימור חיכוך מטא-קוגניטיבי יצרני [81].

### 4.8. פרוטוקול ניתוקי רשת וגבולות עסקאות פדגוגיות (Pedagogical Transaction Boundaries)
*   **שידור Payload מוגבל:** לשמירה על יציבות, המערכת תשדר חבילות Payload קלות משקל (<50KB) אל Firebase רק בנקודות מוגדרות: `block_group_success`, `block_split`, `undo_click` או אירוע `Blur` של שדה קלט עם השהיית Debounce של מעל 1.5 שניות [124].
*   **ניהול ניתוקי רשת (Network Failures):** בעת זיהוי ניתוק רשת או נפילת Wi-Fi בבית הספר, הנתונים הנמצאים בזיכרון הריצה הזמני לא יימחקו בשום אופן [110]. לקוח התלמיד ישמור עד 10 טרנזקציות אחרונות בתוך תור זמני (`In-Memory Queue`) ויסנכרן אותן בשיטת FIFO מול Firebase ברגע חזרת החיבור וביצוע לחיצת יד מוצלחת (Handshake) מול השרת [110, 124].
*   **חיווי חזותי:** בזמן הניתוק, המערכת תקפא חזותית את מסך התלמיד באופן מיידי ותציג הסבר עדין וברור כדי למנוע מהתלמיד המשך פתרון ללא תיעוד פדגוגי [110, 124].

### 4.9. אילוצים וחוקי פריטה מובנים בלוח הדינס הדיגיטלי (Virtual Manipulative Constraint Specs)
*   **מניעת שגיאות פרוצדורליות מובנית:** לוח לבני הדינס הווירטואלי יכלול חוקים ואילוצים קשיחים המונעים מהלומד לבצע פעולות שאינן תקפות מתמטית [569].
*   **איסור המרה במחסר:** חל איסור טכנולוגי וממשקי מוחלט לבצע פעולות פריטה או הקבצה על גבי הלבנים המייצגות את המחסר (Subtrahend) או את המחובר השני בתרגיל [569]. המרת לבנים (באמצעות פטיש או הקבצה) תתאפשר אך ורק על גבי הלבנים של המחובר הראשון או המחוסר (Minuend), ובכך נמנעות שגיאות פרוצדורליות מסיחות ומצומצם העומס הקוגניטיבי על זיכרון העבודה של לומדים עם מוגבלויות [569].

### 4.10. סיווג וניהול חוסר אונים נרכש (Learned Helplessness Detection System)
*   **זיהוי בזמן אמת:** המערכת תנטר ברקע ובאופן שקט את דפוסי העבודה של התלמיד, במטרה לזהות סימנים מוקדמים של חוסר אונים נרכש (כגון השהיות ארוכות, שימוש מופרז ברמזים, מחיקות מרובות ולחיצות אקראיות - Gaming the System) [553, 576].
*   **שילוב פרופיל אקדמי (GWA Integration):** מאחר שמחקרים מראים כי המנבאים החזקים ביותר לחוסר אונים הם הרקע האקדמי הכללי של התלמיד (GWA) ורמות החרדה המוקדמות שלו (ולא רק פעולות הממשק ברגע נתון) [44], המערכת תאפשר למורה להזין מטא-נתונים אלו לדשבורד באופן חסוי. המערכת תכייל אוטומטית את ספי הרגישות של ההתראות (הפעלת חניכה סוקרטית או התראה למורה) בהתאם לפרופיל האישי של התלמיד [44].

---

## 5. אבטחה, אנונימיזציה והפרדת PII (Security & Privacy Specifications)

המערכת מיישמת מדיניות אנונימיזציה קשיחה (No PII) לצורך עמידה ברגולציית אבטחת מידע לילדים מתחת לגיל 13 של משרד החינוך, תוך הפרדה הרמטית בין נתוני הלמידה הפיזיים והלוגיים [18, 88]:

### 5.1. מדיניות איסור שמירה מקומית (Zero Local Storage Policy)
*   חל איסור טכנולוגי מוחלט על שימוש באחסון דפדפן מקומי (`localStorage`, `sessionStorage`) או קובצי קוקיס (`Cookies`) בצד לקוח התלמיד [18, 108].
*   כל נתוני הלמידה והעקבות הדיגיטליים נשמרים אך ורק בתוך זיכרון ריצה זמני (`In-Memory Buffer`), המנקה את עצמו באופן אוטומטי ומוחלט עם סגירת הלשונית, למניעת זליגת מידע במכשירים בית-ספריים משותפים (כגון Chromebooks) [18, 108].

### 5.2. סנכרון מצב זמני (Transient State-Sync)
*   למניעת אובדן נתוני למידה בעת ביצוע רענון לדף (F5) בעיצומו של תרגיל, המערכת מבצעת סנכרון מצב חלקי ושקט (Transient State Sync) אל ענן ה-Backend (Firebase) [18, 109].
*   בכל שינוי של שלב פתרון בתוך תרגיל בודד, המערכת משדרת חבילת נתונים קלת משקל (**Payload קטן מ-50KB**) המכילה את מצב התרגיל הנוכחי בלבד, תחת מזהה אסינכרוני זמני (Anonymous Token) ללא שום פרט מזהה אישי (PII) [18, 109].

### 5.3. בידוד פרטי זיהוי מנהל המערכת (Server-Side Secrets Config & SSO Domain Constraint)
*   **הפרדה מוחלטת של מזהי מורים:** כתובות המורים אינן נשמרות כמשתני סביבה חסויים בשרת. מנהל המערכת מזין אותן באופן דינמי ב-Whitelist (רשימת המורשים) במסד הנתונים בעת הקמת הרישיונות [120, 121].
*   **משתני סביבה סטטיים למנהל בלבד (Admin Server-Side Secrets):** כתובות הדוא"ל האמיתיות של מנהל המערכת (גלעד) יקודדו בצד השרת כמשתני סביבה חסויים ומוצפנים ב-Firebase Cloud Functions Configuration [121]:
    *   `PROCESS.ENV.ADMIN_SSO_PRIMARY_EMAIL` (ממופה לכתובת המבצעית של גלעד: davidsep@edu-haifa.org.il) [121].
    *   `PROCESS.ENV.ADMIN_SSO_ALIAS_EMAIL` (ממופה לכתובת המבצעית המשנית של גלעד: 1002220159@edu-haifa.org.il) [121].
*   **איחוד זהויות (Identity Alias Mapping):** ה-Database יתמוך בקישור ובאיחוד (Mapping) של שני משתני ה-SSO של גלעד תחת מזהה משתמש (UID) פיזי יחיד ומאוחד, המגדיר לו מערך הרשאות כפול: `roles: ["TEACHER", "ADMIN"]` [122].

### 5.4. מנגנון סינון וניקוי PII בצד השרת (Server-Side PII Scrubbing Gateway Middleware)
*   חל איסור מוחלט על לקוח התלמיד לפנות ישירות ל-APIs חיצוניים (כמו ה-API של Gemini) [120].
*   כל בקשת אנליטיקה או תיווך סוקרטי תישלח תחילה אל פונקציית שרת מאובטחת (Firebase Node.js Cloud Function) המריצה מנוע Regex קשיח המנקה באופן אקטיבי (PII Scrubbing) שמות, כתובות מייל, סיסמאות או מזהים אישיים מה-Payload, ורק אז מעבירה את הבקשה האנונימית החוצה [120]. מפתח ה-API של Gemini יישמר בצד השרת בלבד בתוך Firebase Secret Manager [120, 121].

### 5.5. מוגנות דיגיטלית וערוץ תקשורת ישיר (Direct Pedagogical Channel & Safety Spec)
*   **ערוץ תקשורת אנושי סגור:** המערכת תכלול ערוץ הודעות ישיר וסגור המאפשר שיח פדגוגי ישיר (Direct Pedagogical Channel) בין התלמיד למורה שלו, ובין מורי המערכת למנהל המערכת [2, 110]. 
*   **חסימת אינטראקציית עמיתים:** חל איסור טכנולוגי קשיח ומוחלט על קיום שיח חופשי, תקשורת, או כל קשר דיגיטלי אחר בין התלמידים לבין עצמם (Peer-to-Peer Chat), וזאת בהלימה מוחלטת לעקרונות המוגנות הדיגיטלית ומניעת חרדה חברתית ועומס שפתי אצל לומדי ASD ושילוב [2, 110].

### 5.6. ניהול סביבות מרובות מוסדות (Multi-Tenant Scale & Environment Management)
*   **הקמת סביבות על בסיס שם מוסד חינוך:** מנהל המערכת מקים סביבות הוראה מוגדרות למורים בתוך מסד הנתונים בהתבסס על **שם מוסד החינוך** (שם בית הספר).
*   **מגבלות קנה-מידה לפיילוט הנוכחי (Scale Limits Spec):** עבור הרצת הניסוי הנוכחי והאפיון המוגדר במערכת, תעוגן תמיכה קשיחה בקנה המידה הבא:
    *   עד **5 מוסדות חינוך** (5 בתי ספר שונים במערכת).
    *   עד **5 מורים** בסך הכל (מורה מוביל אחד לכל סביבת מוסד).
    *   לכל מורה - ניהול של עד **5 כיתות** (עד 5 כיתות שונות תחת כל מורה בדשבורד שלו).

---

## 6. מדדי ביצוע פדגוגיים מרכזיים (Pedagogical KPIs & Metrics)

הערכת הצלחת המערכת והלמידה המעצבת בה תתבסס על מדדים תהליכיים מעוגנים מחקרית המופקים בזמן אמת מתוך ה-Telemetry השקט [7, 18]:

1.  **יעילות חישובית ותפעולית (Efficiency):** מספר שלבי הפתרון והפעולות הנכונות בממשק המבוצעות על ידי התלמיד באופן עצמאי לחלוטין (ללא קבלת רמזים או תיווך AI אקטיבי) במהלך דקת עבודה [7, 247].
2.  **מדד ההתמדה (Persistence Index):** עלייה בזמן המושקע בפתרון תרגילים מאתגרים (Time on Task) לפני נטישת משימה, לצד ירידה משמעותית בתדירות של מחיקות אקראיות, החלפות תרגיל מהירות וניחושי מקלדת המעידים על שיוט פסיבי (Passive Compliance / Gaming the System) [7].
3.  **מדד העצמאות (Independence Index):** ירידה מובהקת בכמות קריאות העזרה הפיזיות של התלמיד למורה בכיתה, כתוצאה משימוש מושכל ואקטיבי בחונכות הסוקרטית המובנית במערכת המאפשרת פתרון קשיים עצמאי [7].
4.  **איכות הדיאלוג הפדגוגי (Pedagogical Dialogue Quality):** עלייה בשיעור המשובים התהליכיים (Process-oriented feedback) הממוקדים שמעניקה המורה שרה לתלמידים, על בסיס הנתונים המוצגים בדשבורד (לדוגמה: "שרה מזהה בדשבורד שהתלמיד פירק את המאה נכון אך נתקע בטור העשרות" ומעניקה עזרה ספציפית), במקום משובים משימתיים כלליים ומנחשים [7, 446].
5.  **האפקט המשווה (The Equalizing Effect):** צמצום ההטיות הלא-מודעות בהקצאת קשב בכיתה הטרוגנית [8, 446]. באמצעות המידע האובייקטיבי שבדשבורד, המערכת תניע את המורה לחלק את תשומת הלב והמשוב התהליכי בצורה שוויונית בין תלמידים מתקשים (Low-ability) לבין תלמידים בעלי הישגים גבוהים (High-ability) הזקוקים לתמיכה לצורך הצטיינות [446, 466].

---

## 7. התאמות פדגוגיות ומחקריות לחינוך מיוחד (Special Education & ASD Customizations)

*   **דעיכת פיגומים הדרגתית (Scaffold Fading):** כדי למנוע את "פער הדעיכה" (Fading Gap) וצניחת ביצועים המאפיינת תלמידים עם מוגבלויות ברגע שמסירים את עזרי הלמידה הדיגיטליים בפתאומיות [34, 566], המערכת מנהלת עקומות דעיכה מדורגות, משאירה את כפתור ה-Undo פעיל תמיד כעוגן ביטחון רגשי שאינו מוריד ציון [34], ומציגה רמזים סוקרטיים ממוקדים (Faded Abstract Prompting) ללא חזרה לרמה הקונקרטית של הבלוקים [34].
*   **החרגת עיגול מספרים (Curriculum Adjustment for Special Ed):** על פי מתווה משרד החינוך לארגון תכני הוראה בחינוך המיוחד, נושא עיגול המספרים לעשרות, מאות ואלפים ושימוש בסימן ≈ מוגדר כרשות והעשרה בלבד או נדחה באופן קשיח לכיתה ד' [6, 116, 541]. זאת במטרה למנוע עומס קוגניטיבי, תסכול וחרדת ביצוע אצל לומדי ASD ושילוב [6, 116, 541].
*   **אופטימיזציה מוטורית:** כל עזרי העזר והתיווך (כולל לוח החיבור האדפטיבי) יתבססו על אינטראקציית קליקים פשוטה ויקבילו לכללי הנגישות המוטורית (איסור על שימוש במצב Hover המקשה על לומדי ASD וחנ"מ) [81].
*   **גמישות החלת כפתור האפיון של חנ"מ (Class-Level vs. Student-Level Override):** כפתור האפיון הדיגיטלי של חנ"מ (Special Ed Customization Button), השולט בהפעלת מנגנון ה-Strict CRA Bridge וחסימות המקלדת, יהיה בעל גמישות החלה כפולת-רמות. המורה שרה תוכל להפעיל ולכייל אותו לפי הצורך הפדגוגי בשני אופנים:
    1. **ברמת התלמיד הבודד (Student-Level Override):** החלת פרופיל התמיכה המוגבר באופן ספציפי ודיפרנציאלי רק עבור תלמידים מסוימים המוגדרים כמשולבים או חנ"מ.
    2. **ברמת הכיתה כולה (Class-Level Override):** החלת פרופיל התמיכה המוגבר באופן גורף על כלל תלמידי הכיתה בעת הצורך (למשל, בעבודה קבוצתית כיתתית מונחית).

---

## 8. סכמת אירועים לניטור פדגוגי ושחזור (Vector Replay Schema)

לצורך הפעלת נגן השחזור התהליכי האינטראקטיבי (Interactive Session Replay) ככלי אבחון אופליין בצד המורה, המערכת מייצרת עקבות למידה גולמיים קלי-משקל מבוססי שינויי State ב-JSON (נפח קטן מ-50KB לתרגיל בודד) ללא הקלטת וידאו [10, 38, 125]. הסכמה חייבת לתמוך באירועים הבאים: `block_split`, `block_group_success`, `undo_click`, `hesitation_timeout` ו-`socratic_dialogue` [125].

### מפרט סכמת ה-JSON הנדרשת (Vector Replay Event Schema) [125, 126]:

```json
{
  "event_type": "vector_replay",
  "session_id": "session_987f654",
  "timestamp": 1722039917,
  "interaction_data": {
    "action_type": "block_split",
    "details": {
      "block_type": "hundreds_block",
      "target_column": "tens",
      "coordinates": { "x": 105, "y": 200 },
      "duration_ms": 450
    }
  },
  "somatic_indicators": {
    "hesitation_detected": false,
    "undo_triggered": false
  }
}
```

---

## 9. מתווה וקווים מנחים להטמעה ב-Google Antigravity 2.0

כדי להבטיח שסוכן הפיתוח של Antigravity 2.0 יפעל אך ורק בהתאם לאפיון החדש ויפיק קוד תקין, יש להזין לו את מתווה העבודה הבא כחלק מ-System Prompt של הפרויקט:

1.  **Strict Compliance Check:** על הסוכן לסרוק כל רכיב קוד שהוא מייצר או משנה (HTML/CSS/JS) ולוודא שהוא מתאים לחלוטין למכונות המצבים של ה-CRA Bridge ודעיכת הפיגומים המוגדרים בסעיף 4.
2.  **No PII Validation Guard:** חל איסור מוחלט על ה-Agent לייצר לוגיקת צד לקוח השומרת מידע ב-`localStorage` או ב-`sessionStorage`. כל ה-State מנוהל בזיכרון הריצה זמני של האפליקציה (`Vuex`, `Redux` או `Context API`) ומסונכרן דינמית תחת מזהה אנונימי בלבד.
3.  **Route Protection Verification:** על הסוכן לוודא כי הניתוב אל `/admin` מוגן בצד השרת באמצעות ה-Route Guard המאמת את ה-Token מול משתני הסביבה החסויים של ה-SSO המוגדרים ב-Firebase, ולא באמצעות בדיקה מקומית בצד הלקוח.
4.  **Zero-Generation Policy:** הסוכן מוגבל מלחולל טקסט חופשי עבור התלמידים. כל הרמזים, השאלות הסוקרטיות והאפשרויות הסגורות יישלפו אך ורק מתוך מערך נתונים קשיח ומוגדר מראש (Q-Matrix) המובנה בקוד.
